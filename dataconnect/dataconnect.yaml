specVersion: "v1"
serviceId: "smartcare"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "smartcare-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
